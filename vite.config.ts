import { defineConfig } from 'vite'

// Root level vite config for the workspace
export default defineConfig({
  // This config is mainly for Lovable's project detection
  // The actual frontend build uses frontend/vite.config.ts
})
